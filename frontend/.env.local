# URL công khai cho browser (dùng trong next.config.js rewrite)
NEXT_PUBLIC_API_URL=http://localhost:8001

# URL nội bộ cho SSR (gọi trực tiếp backend trong docker network)
INTERNAL_API_URL=http://backend:8001
